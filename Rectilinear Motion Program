//This program will showcase Rectilinear Motion calculation of a particle
//This will have a couple of functions of calculation of the distance, velocity, and acceleration
//This program will also showcase the output the distance of a particle dependency on time 

#include <iostream>
#include <iomanip>
#include <cmath>
using namespace std;
//prototypes 

void info();
void get_info();
double calculation(double result);



int main()
{
int result;
void info();
exponent(result);
}

void info( )
{
cout<<"Welcome! Please enter the values of exponent to calculate, starting with the highest power!"<<endl;
}

double calculation(double result) 
{
double equation;
int num;
cin>>expo1>>expo2;
cout<<"Please enter value for you second highest degree number\n";
cin>>num;
for (int i = num; i<7; i++)
{
equation = pow(value,expo1)+pow(value*num,expo2);
return equation;
}
}
